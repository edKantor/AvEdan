# AvEdan Zephyr module: our own custom boards and device drivers, reusable
# across products on top of Zephyr's own HAL/BSP/driver support. Empty
# scaffold until real hardware/drivers are added.
add_subdirectory(drivers)
